# BootReady — 개발기

> 왜 만들었고, 무엇이 문제였고, 어떻게 풀었는가.

---

## 시작

PC를 켜고 나서 "이제 써도 되는 시점"이 언제인지 항상 불분명했다.  
바탕화면이 떴다고 다 된 게 아니다. 백신, 클라우드 동기화, 런처, 각종 시작프로그램들이 뒤에서 조용히 올라오고 있고, 그 와중에 뭔가를 실행하면 버벅인다.  

그냥 다 끝났을 때 트레이에 아이콘 하나 뜨면 되지 않나 — 그게 시작이었다.  
화려할 필요 없다. 무겁지 않아야 한다. 조용해야 한다.

---

## 1단계: Electron + Rust (두 개의 프로세스)

처음 구조는 직관적이었다.

- **boot-core** (Rust): 부팅 시 자동 실행, 시작프로그램 감시, SQLite 기록
- **bootready-ui** (Electron + React): 트레이 아이콘, 팝업 UI

boot-core는 레지스트리와 시작 폴더를 읽어 시작프로그램 목록을 뽑고, 500ms마다 프로세스 목록을 폴링해서 언제 다 올라왔는지 감지한다. 완료되면 `show.signal` 파일을 생성하고, UI가 그걸 감시하다가 팝업을 띄운다.

구조 자체는 나쁘지 않았다. 문제는 Electron이었다.

### 문제: Electron은 너무 무거웠다

트레이 아이콘 하나 띄우는데 설치 파일이 **80MB**. Chromium이 통째로 들어간다.  
"경량 유틸리티"라는 방향과 정면으로 충돌했다.

### 해결: Tauri v2로 교체

Tauri는 OS의 WebView를 쓰기 때문에 번들에 브라우저 엔진을 넣지 않아도 된다.  
마이그레이션 후 설치 파일 크기: **2.4MB**. 97% 감소.

Named Pipe IPC도 걷어냈다. boot-core와 UI가 같은 SQLite를 공유하면 되는데, 굳이 IPC가 필요하지 않았다. DB 직접 읽기로 단순화했다.

---

## 2단계: 트레이 토글 — 단순해 보이는 게 단순하지 않다

트레이 아이콘 클릭 → 팝업 열기/닫기. 이게 커밋 4개짜리 문제였다.

### 문제: 열자마자 닫힌다

클릭하면 창이 뜨는데, 뜨는 순간 포커스를 얻고 → 동시에 포커스를 잃는 이벤트가 발생해 → 바로 닫힌다.

원인은 타이밍 경합. 트레이 클릭 이벤트와 `focus` / `blur` 이벤트가 거의 동시에 발생하면서 `blur → 자동 숨김` 로직이 먼저 타는 경우가 있었다.

### 해결: 클릭 시점 visibility 추적 + 디바운스

마우스다운 시점의 visibility 상태를 기억해두고, 클릭 완료 시 그 값 기준으로 토글한다. blur 이벤트에는 짧은 딜레이를 걸어 경합을 방지했다.

---

## 3단계: exe_path 잘림 — 데이터가 잘못 저장되다

타임라인 화면을 보면 일부 프로그램이 `C:\Users\Dongchan` 까지만 저장돼 있었다.

### 원인

레지스트리에서 읽어온 실행 경로가 따옴표 없이 공백 포함된 경우 (`C:\Program Files\App\app.exe --flag`), 파싱 로직이 첫 공백에서 잘랐다.

따옴표로 감싸진 경로(`"C:\Program Files\App\app.exe" --flag`)는 문제없었지만, 따옴표 없이 공백 있는 경로가 레지스트리에 꽤 많았다.

### 해결: 파일 존재 확인으로 경계 탐지

토큰을 왼쪽부터 하나씩 붙여가며 `Path::is_file()`로 확인한다.  
파일로 존재하는 시점까지가 exe 경로, 그 이후는 인수다.

```
"C:\Program" → not a file
"C:\Program Files" → not a file
"C:\Program Files\App\app.exe" → file exists ✓
```

레지스트리 값 형식에 방어적으로 대응하면서도 추가 의존성 없이 해결됐다.

---

## 4단계: URL이 부팅 후가 아니라 즉시 열린다

설정에 URL을 등록해두면 부팅 완료 시 브라우저로 자동 열리는 기능을 만들었다.  
그런데 PC를 켤 때뿐 아니라, 낮에 그냥 앱을 실행해도 URL이 열려버렸다.

### 원인

boot-core는 bootready-ui가 실행될 때마다 새로 시작됐다. 오후에 앱을 띄우면 boot-core가 시작프로그램을 읽고 → 이미 다 실행 중이니 즉시 "완료" 판단 → URL 오픈.

### 해결: 3가지 조건으로 "진짜 부팅 직후"만 허용

```
1. 등록된 시작프로그램이 없으면 → skip
2. 시스템 uptime이 10분 초과면 → skip (이미 부팅한 지 한참 됨)
3. 첫 폴링 시점에 절반 이상이 이미 실행 중이면 → skip (부팅 직후가 아님)
```

이 세 조건을 통과해야만 URL을 연다. 오탐 없이 실제 부팅 직후만 정확히 잡는다.

---

## 5단계: 트레이 아이콘이 아예 안 뜬다 — 가장 근본적인 문제

가장 오래 방치됐던 버그. 새 PC에 설치하거나 재부팅하면 트레이 아이콘이 없다.

### 원인

bootready-ui가 시작프로그램으로 등록이 안 돼 있었다.  
설치 후 수동으로 한 번 실행해야 했고, 그다음 부팅부터는 당연히 안 뜬다.

boot-core는 NSIS 설치 시 schtasks로 등록하는 방식이었는데, 이게 불안정했다.

### 해결: 첫 실행 시 HKCU\Run에 직접 등록

앱이 처음 실행될 때 (`config.first_run_done`이 없으면) `BootReady.exe`를 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`에 등록한다. 이후 모든 부팅에서 자동 실행된다.

schtasks 의존 없이, 설치 후 한 번만 실행하면 영구 등록된다.

---

## 6단계: 단일 프로세스로 통합 (v1.1.0)

두 프로세스 구조를 유지하면서 문제가 쌓였다.

- 트레이 아이콘이 두 개 뜨는 버그 (boot-core 완료 후 UI가 또 트레이 생성)
- boot-core 업데이트 시 기존 프로세스 종료 타이밍 문제
- boot-core.exe를 NSIS로 번들링하고, 업데이트 시 강제 교체하는 복잡한 로직

근본 원인은 두 프로세스가 각자 트레이와 상태를 가지려 한 것.

### 해결: boot-core를 bootready-ui 안으로 흡수

boot-core의 핵심 로직(시작프로그램 감시, SQLite 기록, URL 오픈)을 `watcher.rs`로 추출해서 Tauri 백그라운드 스레드로 실행한다. 프로세스는 하나, 트레이도 하나.

부수 효과로 사라진 것들:
- boot-core.exe 배포 및 업데이트 로직
- schtasks 등록
- show.signal을 boot-core가 쓰고 UI가 읽는 IPC 구조
- 트레이 이중 생성 버그

`legacy/boot-core/`로 이동해 히스토리만 보존.

---

## 7단계: 자동 업데이트 알림 (v1.2.x)

새 버전이 나와도 사용자가 알 방법이 없었다. 그렇다고 백그라운드에서 자동 설치는 앱 철학과 안 맞는다 — 조용해야 하고, 사용자가 통제권을 가져야 한다.

### 설계

- 앱 시작 30초 후 GitHub API로 최신 릴리즈 버전 확인 (1회)
- 현재 버전보다 높으면 팝업 상단에 배너 표시
- 배너 클릭 시 GitHub 릴리즈 페이지 오픈
- 자동 설치 없음

처음엔 12시간마다 반복 확인 로직을 넣었지만, BootReady는 매 부팅마다 시작하는 앱이다. 매 부팅 1회 확인으로 충분하다. 반복 루프 제거.

---

## 돌아보며

처음 "트레이에 아이콘만 뜨면 된다"에서 출발해서, 실제로 제대로 동작하게 만드는 데 생각보다 많은 구석이 있었다.

가장 어려운 건 코드가 아니었다. 언제 URL을 열어야 하는지, 언제가 "진짜 부팅 직후"인지, 두 프로세스가 정말 필요한지 — 이런 판단들이 결국 코드의 복잡도를 결정했다.

기능을 빼는 것도 결정이다. 파일 아이콘 추출(SHGetFileInfoW)은 COM 초기화, 환경변수 미확장, 경로 잘림 등 엣지케이스가 너무 많아서 제거했다. 없는 게 낫다고 판단했다.

지금은 설치 파일 2.4MB, 단일 프로세스, 트레이 아이콘 하나. 처음 원하던 그대로다.

---

## 버전별 요약

| 버전 | 변경 |
|---|---|
| 0.1.x | Electron + boot-core 초기 구조, 트레이/타임라인/히스토리 골격 |
| 0.1.5 | Tauri v2 마이그레이션 (80MB → 2.4MB) |
| 0.1.6 | COM 초기화, 환경변수 확장 시도 후 아이콘 기능 제거 |
| 0.1.8 | 부팅 완료 시 URL 자동 오픈 |
| 1.0.x | 트레이 이중 생성 / 팝업 경합 / boot-core 업데이트 버그 수정 |
| 1.1.0 | boot-core 흡수 → 단일 프로세스, 첫 실행 시 자동시작 등록, URL 오픈 가드, exe_path 잘림 수정 |
| 1.2.x | 자동 업데이트 알림 (GitHub API, 1회 확인) |
